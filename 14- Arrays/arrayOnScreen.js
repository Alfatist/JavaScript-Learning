let values = [8, 1, 7, 4, 2, 9];

/* for (let i = 0; i < array.length; i++) {
  console.log(`Index ${i} have the value ${values[i]}`);
} */

for (let i in values) {
  console.log(`Index ${i} have the value ${values[i]}`);
}
